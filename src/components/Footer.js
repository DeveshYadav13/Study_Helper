// Footer.js
import React from 'react';
import { Container, Typography, Box, Paper, Grid } from '@mui/material';

const Footer = () => {
    return (
        <Container style={{ padding: 0 }}>
        <footer style={{ textAlign: 'center', padding: '15px', backgroundColor: '#333', color: '#fff', fontFamily: 'Arial, sans-serif', fontSize: '16px', marginTop: 'auto', width: '100%', position: 'fixed', left: 0, bottom: 0 }}>
            Made in frustration by DY
        </footer>
      </Container>
      
    );
};

export default Footer;
